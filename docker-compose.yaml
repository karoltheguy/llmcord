services:
  container:
    build: .
    network_mode: host
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    volumes:
      - ./:/app:ro
      # The bind above is read-only, so memory needs its own writable mount.
      - ./data:/app/data
